# Install Razorpay SDK

Run this command in the backend directory:

```bash
cd apps/backend
pnpm add razorpay
pnpm add -D @types/razorpay
```

Razorpay SDK is required for payment gateway integration.
