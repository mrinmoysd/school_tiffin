# Install AWS SDK for S3

Run this command in the backend directory:

```bash
cd apps/backend
pnpm add @aws-sdk/client-s3
pnpm add -D @types/multer
```

AWS SDK v3 is required for file uploads to S3.
